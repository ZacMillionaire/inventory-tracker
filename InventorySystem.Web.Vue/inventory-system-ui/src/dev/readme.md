# Dev Tools
Designed to only be available during local dev, provides style guide and css variable editors

## Routing
Uses in memory routing and is mounted as an individual Vue app during `npm run dev`

## TODO
- Inline styling and components so it doesn't use application components
- Clean up the styling so it doesn't feel so hacky as a full height div
- Clean up styling for css variable editor because it has weird interactions with its width